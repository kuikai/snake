# Paste this into Cursor (Home mode chips only)

The Home background is good. Leave it.

The mode/size chips at the top feel all over the place. Rebuild that picker only.

Layout, top to bottom:
1. Wordmark + best
2. Empty space
3. Mode row
4. Size + Speed row
5. Play
6. Settings / History
7. Snake + apple art at the bottom (do not cover)

Mode row — one selection:
`Classic`  `Wrap`  `No Walls`  `Obstacles`

Size row:
`Small`  `Medium`  `Large`     and a compact `Speed` toggle on the right

Rules:
- Do not mix modes and sizes in one row
- Do not put this bar under the status bar / at the top of the screen
- Labels are the names only. No “Pro” suffix
- Locked chips get a small lock icon only
- Free: Classic + Medium selected. Wrap, No Walls, Obstacles, Small, Large, Speed are locked
- Tap any locked chip → the same paywall. One $1.99 unlocks all of them
- If chips overflow, horizontal scroll. No wrapping into a messy second wrap of mixed chips
- Quiet paper-tag style, not dark Material filter chips

Do not change the engine or the Game screen.
